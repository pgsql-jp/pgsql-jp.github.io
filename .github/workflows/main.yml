name: jpug-doc build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - name: Checkout jpug-doc
      uses: actions/checkout@v2
      with:
        repository: pgsql-jp/jpug-doc
        path: tmp-jpug-doc
